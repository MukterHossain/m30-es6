const first = 'Jaan'
const last = 'Pakhi'
const greet = 'book pen'
const name = first+ ' ' + last + ' ' + greet;
console.log(name);

const a = 10;
const b = 20;
const result = 'the sum of ' + a + ' and ' + b + ' is ' + (a + b);
console.log(result)

const numbers = [45, 48, 98, 74];
const student = {name: 'hamun', age:5};


const math = `the sum of ${numbers[1]} and ${student.age} is ${numbers[1] + student.age}`
// const math = `the sum of ${a} and ${b} is ${a + b}`
console.log(math)

const email = 'Hi john \n' + 
'cena cena lage \n' + 
'you are very good person'   // javaScript এ নতুন লাইন লিখতে n\ + ব্যবহার করা হয়।

console.log(email)

const challenge = `John Cena   
Cena laki  are good
I am very miss you`     
console.log(challenge)  // javaScript এ নতুন লাইন লিখতে ` ` ব্যবহার করা হয়।